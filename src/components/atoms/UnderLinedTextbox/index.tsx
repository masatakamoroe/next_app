import React from "react";
import styles from "./styles.module.sass";

const UnderLineTextbox: React.VFC = () => {
    return <input className={styles.container} type="text" />;
};

export default UnderLineTextbox;
